import React from 'react'
import { StyleSheet, View, Text } from 'react-native'

export default function SelectHall({props}) {
    console.log(props)
  return (
    <View>
      <Text> $2 </Text>
    </View>
  )
}

const styles = StyleSheet.create({})